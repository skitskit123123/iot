int sensorPin = A0;
int sensorValue = 0;
int led=13;
void setup()
{
Serial.begin(9600);
pinMode(13, OUTPUT);
}
void loop()
{
sensorValue = analogRead(sensorPin);
Serial.println(sensorValue);
if (sensorValue<400)
digitalWrite(13, HIGH);
else
digitalWrite(13, LOW);
delay(1000);
}
